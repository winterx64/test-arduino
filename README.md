# USMP Demo (Arduino Library)

Arduino C++ library for USMP (Unified Secure Multi-transport Protocol).

## Installation

Search for `USMP_Demo` in the Arduino Library Manager, or install via ZIP.

## Example

See `examples/Echo/Echo.ino`.
